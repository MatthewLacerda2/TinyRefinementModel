# Model card — `run_20260720_012843`

**One line:** Notable challenger — the `reasoner` control arm of the #16 matched pair.
Kept only because it is the *matched* half of a comparison; on its own it is a plain
random-depth transformer with a documented-inert depth knob.

## Recipe (what regenerates it)

| Field | Value |
|---|---|
| Commit SHA | `3eb4bae892c647bb71f66cf3e64731321ef95768` (**`git_dirty: true`**) |
| `MODEL_ARCH` | `reasoner` (`UniversalReasoner`) |
| Config snapshot | identical to the refiner arm: `LATENT_DIM=960, NUM_BLOCKS=8, NUM_HEADS=15, NUM_GROUPS=3, SHARED_SLOTS=32, MAX_SEQ_LEN=512, MAX_STEPS_LIMIT=8, BATCH_SIZE=1, ACCUMULATION_STEPS=128` |
| Params | 138.4M |
| Tokenizer | `r50k_base` (VOCAB_SIZE `50304`, PAD `50256`) |
| Dataset manifest | same as the refiner arm — `runs/data/pretrain`, fineweb-edu / codeparrot / finemath |
| Seed(s) | `DATA_SEED=42`, `MODEL_SEED=42` — **same as the refiner arm**, which is what makes this a matched pair |
| Optimizer / schedule | `TRAIN_TOKEN_BUDGET=200000000`, `DECAY_STEPS=1526`; f16 compute |

## Result (how it did)

| Metric | Value | Noise floor (seed σ) |
|---|---|---|
| Held-out val CE (step 1540) | **4.9182** (ppl ≈ 137) | σ ≈ 0.03 nats (#17) |
| Train CE (step 1540) | 4.4268 — *lower* than the refiner's 4.4778 | — |
| Train→val gap | 0.491 vs the refiner's 0.417 | gap difference is itself sub-noise |
| GPT-2-small yardstick | far below — not a fair test at 101M tokens | — |

## Cost

| | |
|---|---|
| Peak VRAM | not instrumented; needed the same three env knobs as the refiner arm |
| Wall-clock | 15.7 h |
| Tokens seen | 101M (step 1536 × 128 accum × 512 seq) |

## Weights (the regenerable cache)

| | |
|---|---|
| Checkpoint path (live) | `runs/run_20260720_012843/checkpoints/196607` (= opt step 1536) |
| Archive path (HDD) | `/mnt/d_drive/TRM_cold/weights/control_armB_step1536/` |
| `sha256` | `729a538ca774126e169ea605a691008ea5d94af2147d5d906dcbf8196d79737f` (hash of the per-file `SHA256SUMS` manifest stored beside the weights; **regenerated 2026-08-25** when the archive was relaid out to the loadable layout — file contents unchanged, only paths) |

## Notes

- Its depth knob is **inert** — depths {1,2,4,8,16} produce byte-identical output
  (`docs/findings/2026-06-13-cross-window-hunch-inert.md`), and it ran ~5× slower per
  token spending compute on a mechanism that changes nothing. That is exactly why it is
  the control: a vanilla transformer wearing the same harness.
- It fit the training set slightly *better* than the refiner yet generalized slightly
  *worse*. Directionally what the sample-efficiency hypothesis predicts — but sub-noise
  and single-seed, so it is a hint to test at scale, not evidence.
- Champion / matched arm: `docs/registry/run_20260719_042625-refiner-base-v1.md`.
